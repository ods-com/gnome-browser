import javax.swing.JFrame;

public class ReadFileMain {
    public static void main(string[] args) {
        ReadFile system = new ReadFile();
        system.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
